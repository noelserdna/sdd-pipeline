# Plan Templates — Output Artifact Formats

> Reference document for sdd-plan-architect Phase 5 (Plan Generation) and Phase 4 (Architecture Design).
> Contains canonical templates for all output artifacts.

---

## Template: PLAN.md (Master Implementation Plan)

Budget ≤ 12 000 chars. Technology rows are `aspect · decision · reference` (the ADR holds the rationale); cross-FASE concerns ≤ 5 lines each; quickstart ≤ 15 lines; traceability tables carry ids only.

```markdown
# Implementation Plan — {System Name}

> Generated by sdd-plan-architect from spec v{version}
> Date: {YYYY-MM-DD}
> Mode: {Global | Per-FASE}

## Document History

| Version | Date | Changes |
|---------|------|---------|
| 1.0 | {date} | Initial plan generation |

---

## 1. Executive Summary

{2-3 sentences: what this plan covers, key architectural decisions, implementation approach}

## 2. Technical Context

### 2.1 Platform & Runtime

| Aspect | Decision | Reference |
|--------|----------|-----------|
| Runtime | {e.g., Cloudflare Workers} | {ADR-xxx} |
| Language | {e.g., TypeScript 5.x} | {ADR-xxx} |
| HTTP Framework | {e.g., Hono} | {ADR-xxx} |
| Build Tool | {e.g., Wrangler} | {ADR-xxx} |

### 2.2 Storage

| Store | Technology | Purpose | Reference |
|-------|-----------|---------|-----------|
| Primary DB | {e.g., Cloudflare D1} | Relational data | {ADR-xxx} |
| Object Store | {e.g., Cloudflare R2} | PDF/document storage | {ADR-xxx} |
| Cache/KV | {e.g., Cloudflare KV} | Rate limiting, sessions | {ADR-xxx} |

### 2.3 External Integrations

| System | Protocol | Purpose | Reference |
|--------|----------|---------|-----------|
| {e.g., Anthropic API} | {HTTPS/REST} | {LLM extraction} | {contract ref} |

### 2.4 Key Constraints

| Constraint | Value | Source |
|------------|-------|--------|
| {e.g., Max PDF size} | {50 MB} | {nfr/LIMITS.md} |
| {e.g., Extraction timeout} | {360s} | {RN-181} |

## 3. Component Decomposition

### 3.1 Module Map

| Module | Bounded Context | Responsibility | Key Specs |
|--------|----------------|---------------|-----------|
| {e.g., extraction} | Extraction | PDF processing pipeline | UC-001..UC-005 |
| {e.g., cv-analysis} | CV Analysis | 24-dimension analysis | UC-010..UC-015 |

### 3.2 Module Dependencies

```
{ASCII dependency diagram}
extraction → cv-analysis → matching
                         ↘ gdpr
auth ──────→ all modules
```

### 3.3 Shared Components

| Component | Used By | Purpose |
|-----------|---------|---------|
| {e.g., auth-middleware} | All modules | JWT validation, RBAC |
| {e.g., tenant-context} | All modules | Multi-tenant isolation |
| {e.g., error-handler} | All modules | Consistent error responses |

## 4. Cross-FASE Concerns

### 4.1 Authentication & Authorization

{How auth is implemented across phases, when RBAC is introduced}

### 4.2 Multi-Tenant Isolation

{Data isolation strategy, how org_id propagates}

### 4.3 Error Handling Pattern

{Consistent error response format, error codes, logging}

### 4.4 Observability

{Logging format, metrics, tracing strategy}

## 5. Risk Assessment

| # | Risk | Impact | Probability | Mitigation |
|---|------|--------|-------------|------------|
| R1 | {description} | {H/M/L} | {H/M/L} | {strategy} |

## 6. Developer Quickstart

### 6.1 Prerequisites

```bash
# Required tools
{tool installation commands}
```

### 6.2 Setup

```bash
# Clone and setup
{setup commands}
```

### 6.3 Development Workflow

```bash
# Build
{build commands}

# Test
{test commands}

# Deploy (local/dev)
{deploy commands}
```

## 7. Validation & Traceability

### 7.1 UC Coverage

| UC | Module | FASE | Plan Section |
|----|--------|------|-------------|
| UC-001 | extraction | FASE-1 | 3.1 |

### 7.2 ADR Compliance

| ADR | Reflected In | Status |
|-----|-------------|--------|
| ADR-001 | Section 2.1 | ✅ |

### 7.3 NFR Strategy Coverage

| NFR | Strategy | Plan Section |
|-----|----------|-------------|
| {nfr ref} | {strategy summary} | {section} |

### 7.4 Invariant Enforcement

| Invariant | Enforcement Mechanism | Module |
|-----------|----------------------|--------|
| INV-xxx | {e.g., DB constraint + middleware check} | {module} |

---

> Generated by sdd-plan-architect | Spec v{version} | {date}
```

---

## Template: ARCHITECTURE.md

Budget ≤ 10 000 chars. Decisions are cited by id (`ADR-NNN`, `D-PA-NNN`, `RES-NNN`), never paraphrased. Each view is one ASCII diagram plus one id-bearing table; a view the specs give no content to is one line ("No external systems: `contracts/` lists none"), not a section.

```markdown
# Architecture — {System Name}

> Spec v{version} · {YYYY-MM-DD} · Style: {modular monolith | serverless | …} ({ADR ids})
> Inputs: spec/ · design/ {yes|no} · ux/ {yes|no} · audits/ {yes|no}

## 1. Decisions

| Aspect | Decision | Ref |
|--------|----------|-----|
| Runtime / language | {e.g. Node ≥ 18, TypeScript 5.x} | ADR-001 |
| Topology | {e.g. modular monolith, one Worker} | ADR-xxx |
| Storage | {e.g. D1 + R2; JSON file with atomic rename} | ADR-xxx |
| Auth | {none | JWT + RBAC} | ADR-xxx / nfr/SECURITY.md |
| Error model | {classes → HTTP status / exit codes} | ADR-xxx |
| Plan-level | {decision taken by this skill} | D-PA-NNN / RES-NNN |

## 2. C4 Views

### 2.1 System Context (L1)

```
{ASCII: actors → system → external systems}
```

| Element | Type | Interaction (≤ 80 chars) | Ref |
|---------|------|---------------------------|-----|
| {actor} | Person | {what it does with the system} | UC ids |
| {external system} | System | {protocol · data} | contract id |

### 2.2 Containers (L2)

```
{ASCII: deployable units + protocols}
```

| Container | Technology | Purpose (≤ 80 chars) | Ref |
|-----------|-----------|-----------------------|-----|
| {container} | {tech} | {purpose} | ADR-xxx |

### 2.3 Components (L3)

```
{ASCII tree: module/ → component (block letter when the FASE has parallel blocks)}
```

| Component | Module | Responsibility (ids) | Key interfaces | FASE / block |
|-----------|--------|----------------------|----------------|--------------|
| `{path}` | {module} | UC-/INV-/RN- ids | {function or route ids} | FASE-{N} {A|B} |

## 3. Deployment

```
{ASCII topology and environments}
```

| Environment | Purpose | Infrastructure | Entry |
|-------------|---------|----------------|-------|
| Development | Local dev | {tool} | {url or command} |
| Production | Live | {infra} | {url} |

Pipeline: `{lint → test → build → deploy staging → smoke → deploy prod}` (one line).

## 4. Data Model

One schema block per aggregate (from `02-ENTITIES.md` / `03-VALUE-OBJECTS.md`); indexes from UC query patterns; migration strategy in ≤ 5 lines.

```sql
CREATE TABLE {table} ( … );
CREATE INDEX idx_{table}_{col} ON {table}({col});
```

| Data | Store | Format | Retention |
|------|-------|--------|-----------|
| {data class} | {store} | {format} | {policy} |

## 5. Integration Map (only if external systems exist)

| External system | Direction | Protocol | Auth | Error handling | Ref |
|-----------------|-----------|----------|------|----------------|-----|
| {system} | {in/out} | {REST/webhook} | {auth} | {retry/circuit breaker} | contract id |

## 6. Security & Error Handling

```
{ONE ASCII flow: request/argv → validation → auth (if any) → handler → error mapping → response}
```

| Data class | At rest | In transit | Access control | Ref |
|-----------|---------|-----------|----------------|-----|
| {PII / documents / …} | {encryption} | {TLS / none} | {rule} | SEC-xxx |

Error mapping: cite the ADR that defines it (`ADR-NNN §N`); list only what the ADR does not say.

## 7. Quality Attributes

| Attribute | Target | Strategy (≤ 80 chars) | Ref |
|-----------|--------|------------------------|-----|
| {performance} | {p95 < N ms} | {strategy} | SPEC-PERF-001 |

---

> Generated by sdd-plan-architect | Spec v{version} | {date}
```

---

## Template: RESEARCH.md

Two variants. Phase 3 skipped (no genuinely open item and no `--research`): the table only. Phase 3 run: one section per item, ≤ 1 500 chars each; alternatives ≤ 4 rows with pros/cons ≤ 80 chars; no interface sketches (PLAN-FASE §4 owns them); an empirical check is one line with the result.

**Skipped variant:**

```markdown
# Technical Research — {System Name}

> Phase 3 skipped: {no NEEDS_RESEARCH items | all items decided from ADR / spec evidence}. Force it with `/sdd-plan-architect --research`.

| Item | Category | Decided by | Decision (≤ 80 chars) | Ref |
|------|----------|------------|------------------------|-----|
| {question ≤ 80 chars} | CL-xxx | ADR-NNN / RN-NNN / RQ-NNN / TEST-PLAN R-N | {decision} | CLARIFY-LOG D-PA-NNN |

(≤ 5 rows)
```

**Run variant:**

```markdown
# Technical Research — {System Name}

> Generated by sdd-plan-architect Phase 3 · {YYYY-MM-DD} · Agents: TECH ({N} items), PATTERN ({N} items)

## Research Items

### RES-{NNN}: {Title}

**Category:** {CL-xxx} · **Question:** {Original clarify question} · **Status:** {Decided | Open | Deferred}

| Alternative | Pros | Cons | Fit (1-5) |
|------------|------|------|-----------|
| {option 1} | {≤ 80 chars} | {≤ 80 chars} | {score} |
| {option 2} | {≤ 80 chars} | {≤ 80 chars} | {score} |

**Selected:** {chosen} · **Rationale:** {one sentence} · **Trade-offs:** {one sentence}
**Verified:** {one line with the result, if an empirical check was run}
**Refs:** {ids, URLs}

---

## Flagged ADR Drafts

### Draft ADR-{NNN}: {Title}

**Status:** Draft · **Context:** {≤ 3 lines} · **Decision:** {≤ 3 lines} · **Consequences:** {≤ 3 lines} · **Action:** formalize in spec/adr/ via `sdd-spec-auditor --fix`

---

> Generated by sdd-plan-architect | {date}
```

---

## Template: CLARIFY-LOG.md

Budget ≤ 6 000 chars when no question was asked, + ≤ 1 200 per real question. A Q-block exists only for a question presented to the user. Decisions taken by the skill (under `--skip-clarify`, or settled by ADR / spec / design evidence) are table rows — no options table, no restated ADR content.

```markdown
# Clarify Log — {System Name}

> Generated by sdd-plan-architect Phase 2 · {YYYY-MM-DD} · Mode: {interactive | --skip-clarify}

## System Vision Gate

> {System Vision Statement — 3-5 lines}

| Dimension | Status | Evidence |
|-----------|--------|----------|
| Delivery Channels | ✅ Covered / ⚠️ Implicit / ❌ Missing | {ADR-xxx / spec ref} |
| Data Strategy | … | … |
| Auth Model | … | … |
| Integration Points | … | … |
| Quality Attributes | … | … |

## Coverage Summary

| Category | Status | Evidence |
|----------|--------|----------|
| CL-TECH | {Resolved/Partial/Missing} | {ADR / CLAUDE.md / design ref} |
| CL-DATA | … | … |
| CL-UI | … | … |
| CL-ARCH | … | … |
| CL-SEC | … | … |
| CL-INTEG | … | … |
| CL-PERF | … | … |
| CL-TEST | … | … |
| CL-DX | … | … |
| CL-CICD | … | … |
| CL-ENV | … | … |
| CL-OBS | … | … |
| CL-COST | … | … |

**Resolved:** {N}/13 · **Partial:** {N}/13 · **Missing:** {N}/13

## Decisions Without a Question

| ID | Category | Decision (≤ 140 chars) | Evidence / ref | Needs ADR |
|----|----------|-------------------------|----------------|-----------|
| D-PA-001 | CL-DX | {decision} | ADR-002 §2, TEST-PLAN R-1 | No |
| D-PA-002 | CL-TEST | {decision} | RQ-002 (recommended option) | No |

## Session {YYYY-MM-DD} (only if questions were asked)

### Q1: {CL-xxx-NNN} — {Short Title}

**Question:** {Full question text}

| Opción | Descripción |
|--------|-------------|
| {option 1} (Recomendado) | {≤ 100 chars} |
| {option 2} | {≤ 100 chars} |

**Answer:** {selected option} · **Rationale:** {one sentence} · **Impact:** {ids affected} · **Needs ADR:** {No | Yes → ADR-xxx} · **Needs Research:** {No | Yes → RES-xxx}

## Coverage Gate (Phase 2.9)

| Dimension | Resolved by | Status |
|-----------|-------------|--------|
| {dimension} | {ADR / D-PA / Q id} | PASS / Deferred |

**Result:** {PASS | PASS with warnings | BLOCK} · Questions asked: {N} · Decisions: {N} · ADRs needed: {ids} · Research: {ids}

---

> Generated by sdd-plan-architect | {date}
```

---

## Template: PLAN-FASE-{N}.md (Per-FASE Plan)

Budget ≤ 9 000 chars. Signatures, not bodies; ids, not assertions. §7.4 Coverage Map keeps its exact format — it is consumed by `sdd-task-generator` (one test task per row, V-13/V-14) and by `sdd-task-implementer` (CHECK-COV).

```markdown
# Implementation Plan — FASE-{N}: {FASE Title}

> Spec v{version} · {YYYY-MM-DD} · Depends on: {FASE-X} · Enables: {FASE-Y}
> Blocks: {A: module ∥ B: module → Integración} (see FASE-{N} §Módulos y Conjuntos de Escritura)

## 1. FASE Overview

**Objective:** {one sentence} · **Scope:** {modules} · **Delivers:** {contract ids}

## 2. Specs to Implement

| Spec | Type | Implementation note (≤ 100 chars) |
|------|------|-----------------------------------|
| UC-{NNN} | UC | {note} |
| ADR-{NNN} | ADR | {note} |

## 3. Technical Decisions (FASE-Specific)

| ID | Decision (≤ 140 chars) | Ref |
|----|-------------------------|-----|
| {D-PA-NNN / RES-NNN} | {decision} | {ADR / CLARIFY-LOG} |

## 4. Component Implementation

### 4.1 `{path/to/file}` ({block})

**Responsibility:** {UC-/API-/INV-/RN- ids} · **Refs:** {ADR ids}

```typescript
// signatures derived from {contract id} — no bodies
export function {name}({params}): {return};
```

**Notes (≤ 5, only what the contract does not say):**
- {ordering / error precedence / injection point / invariant enforced here}

### 4.2 `{next file}` ({block})

{same format}

## 5. API Implementation Notes

| Endpoint / function | Handler (file) | Middleware / validation | Note (≤ 80 chars) |
|---------------------|----------------|-------------------------|--------------------|
| {API id} | `{path}` | {auth, schema} | {note} |

## 6. Data Changes

{One line when nothing changes. Otherwise:}

| Migration | Description | Reversible |
|-----------|-------------|-----------|
| {NNN}_{name} | {≤ 80 chars} | {Yes/No} |

## 7. Test Strategy

### 7.1 Unit Tests

| Component | Test ids (test/) | Block |
|-----------|------------------|-------|
| `{file}` | PROP-001, UNIT-T-001, TEST-MATRIX-UC-001 T08..T17 | A |

### 7.2 Integration Tests

| Scenario | Test ids (test/) | Setup | Block |
|----------|------------------|-------|-------|
| {scenario ≤ 80 chars} | INT-T-001, AC-006-01..14 | {fixture / mock} | A / C |

### 7.3 BDD / E2E Scenarios

| Source | Scenarios | Tier / Priority | Block |
|--------|-----------|-----------------|-------|
| `tests/BDD-UC-{NNN}.md` | AC-{NNN}-01..NN | P0/P1 | C |
| `test/E2E-SCENARIOS.md` | Smoke: {ids} · Critical: {ids} · Full: {ids} | per tier | C |
| `test/PERF-SCENARIOS.md` | PERF-001..002 | blocking | C |

### 7.4 Test Coverage Map (Source → Test)

| Source File | Type | Test File | Priority |
|------------|------|-----------|----------|
| {source_path} | {logic/entity/service/state-machine/page/component} | {test_path} ({block}) | {HIGH/MEDIUM/LOW} |

**Exclusions (no unit test needed):**

| File | Reason |
|------|--------|
| {file_path} | {infrastructure wrapper / enum constant / tested via integration or E2E} |

> Every source file with testable logic MUST appear in this table or in Exclusions.
> This map drives task generation: each row becomes a test task.

## 8. Dependencies on Shared Components

| Component | From FASE | Required for |
|-----------|-----------|--------------|
| {component} | FASE-{X} | {what needs it} |

## 9. Acceptance Criteria

- [ ] {criterion — UC/INV ids} (≤ 120 chars each)
- [ ] All BDD scenarios of §7.3 pass
- [ ] No new security findings
- [ ] Performance targets met: {PERF ids}

---

> Generated by sdd-plan-architect | Spec v{version} | {date}
```

---

## Format Conventions

### Consistent Across All Templates

1. **Header**: Always includes system name, spec version, generation date
2. **Footer**: Always includes generator attribution
3. **Tables**: Use markdown tables for structured data
4. **Code blocks**: Use fenced code blocks with language tags
5. **ASCII diagrams**: Preferred over external diagram tools (portable, version-controllable)
6. **References**: Always link to source spec/ADR/INV by ID
7. **Status indicators**: Use ✅ ❌ for boolean, {H/M/L} for severity/probability

### Naming Conventions

| Artifact | File Name | Location |
|----------|-----------|----------|
| Master Plan | `PLAN.md` | `plan/` |
| Architecture | `ARCHITECTURE.md` | `plan/` |
| Clarify Log | `CLARIFY-LOG.md` | `plan/` |
| Research | `RESEARCH.md` | `plan/` |
| Per-FASE Plan | `PLAN-FASE-{N}.md` | `plan/fase-plans/` |

### Incremental Updates

When updating existing artifacts:
1. Add new version entry to Document History table
2. Mark changed sections with `<!-- updated {date} -->`
3. Do NOT delete previous content — mark as superseded if needed
4. Update Coverage/Traceability tables to reflect changes
